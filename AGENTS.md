# Agent Instructions

Work locally and make no network requests unless the user explicitly asks.

No telemetry, no analytics and no tracking.

Keep credentials out of outputs, commits, logs and prompts. Rotate any credential accidentally committed to Git.

Do not overwrite files unless the user explicitly asks or passes a force option.
